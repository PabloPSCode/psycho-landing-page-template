import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:{
    domains: ['images.unsplash.com', 'plus.unsplash.com', 'www.kabum.com.br', 'firebasestorage.googleapis.com'],
  }
};

export default nextConfig;
